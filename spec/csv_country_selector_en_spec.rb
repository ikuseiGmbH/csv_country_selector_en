require "spec_helper"

RSpec.describe CsvCountrySelectorEn do
  it "has a version number" do
    expect(CsvCountrySelectorEn::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
